# AGENTS.md

このファイルは、本リポジトリで作業するすべての AI コーディングアシスタント（Antigravity、Claude Code、Cursor、Windsurf、GitHub Copilot、Cline、Codex 等）および開発者向けの共通ガイドラインです。

---

## 🚨 PR 作成前の必須ルール（最重要）

コードを変更して PR を作成・提出する際は、**以下のステップを必ずすべて実施してください。省略は認められません。**

### 必須チェック手順

1. **型チェック・Lint・テスト・ビルドの確認**:
   ```bash
   npm run lint        # ESLint エラー・警告ゼロ
   npm test            # 単体テスト全件通過
   npm run build       # Next.js 本番ビルド（型チェック含む）通過
   ```

2. **コード健康診断（共通基盤チェック）の実行**:
   ```bash
   npm run code-health:diff
   ```
   - ⚠️（悪化）が検出された場合は、必ず「悪化した箇所」を修正してください（既存の共通コンポーネント・ユーティリティを使う、コピペを排除するなど）。
   - 実行後に出力される `.code-health/diff.md` の内容を、**PR 本文の「## 共通基盤チェック」欄に必ずそのまま貼り付けてください**。
   - ※やむを得ず悪化を残す場合は、その欄に「悪化を残す理由・今後の対応方針」を明記してください。詳細は `docs/CODE_HEALTH.md` を参照。

3. **未リリース変更メモ（フラグメント）の作成**:
   - 来訪者から見て変化がある機能追加・UI変更・修正の場合は、`docs/changelog-unreleased/<PR番号またはブランチ名>.md` を作成し、来訪者視点の変更内容を箇条書きで1〜数行記載してください（例: `- 地図の店舗写真を軽量なサムネイルで表示するようにした (#700)`）。
   - 内部的なリファクタ、依存更新、テスト追加など来訪者に見えない変更の場合は作成不要です。

4. **PR テンプレート（`.github/pull_request_template.md`）の遵守**:
   - PR 本文は必ずテンプレートの各項目（概要、主な変更、変更ファイル、動作確認、**共通基盤チェック**）を漏れなく埋めて作成してください。

---

## プロジェクト概要

- **プロダクト名**: nicchyo（ニッチョ）
- **目的**: 高知・日曜市（毎週日曜開催の大規模路上市場）を案内するデジタルマッププラットフォーム。
- **主要機能**: インタラクティブ地図（MapLibre / Leaflet）、店舗検索、出店者マイページ、AI案内（にちよさん）。

## コーディング規約・設計思想

- **共通基盤を最優先で利用する**:
  - 同じロジックやUIパターンを重複して個別実装しない。
  - 共通ロジックは `lib/`、共通UIコンポーネントは `components/ui/`（`@/components/ui`）を使用する。
  - Supabase クライアントの個別生成を避け、共通の認証・クライアント関数を利用する。
- **デザインシステム（`docs/DESIGN_SYSTEM.md`）の厳守**:
  - 生の hex カラーや、新しい `slate-*` / `gray-*`、`rounded-2xl` などの直書きを避け、Tailwind のトークンや共通スタイルを使用する。
- **PR は小さく保つ**:
  - 1つの PR には 1つの目的。変更ファイル数の目安は 10件以内。

---

## 主なコマンド

```bash
npm run dev                 # 開発サーバー起動
npm run build               # 本番ビルド（型チェック含む）
npm run lint                # ESLint
npm test                    # 単体テスト
npm run code-health         # コード健康診断レポート出力（.code-health/report.html）
npm run code-health:diff    # develop 分岐点との比較・悪化チェック（.code-health/diff.md）
npm run changelog:pack      # リリース時: フラグメントの集約
```
